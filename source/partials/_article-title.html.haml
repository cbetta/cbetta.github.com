%h2.ui.center.aligned.icon.header.article-icon
  %i.icon{class: article.data.icon }

%h1.ui.centered.aligned
  = article.title

.ui.one.column.grid.article-info
  .row
    .column.center.aligned
      = link_to blog_month_path(article.date.year, article.date.month), class: 'ui tag label' do
        %i.calendar.icon
        = article.date.strftime('%B %e %Y')

      - article.tags.each do |tag|
        = link_to tag_path(tag), class: 'ui tag label' do
          = tag
