<span class='article__date'>
  <%= article.date.strftime('%A %B %e, %Y') %>
</span>
<h1 class='article__title'>
  <%= link_to article.title, article %>
</h1>
<span class='article__tags'>
  <% article.tags.each do |tag| %>
    <%= link_to "&lt#{tag}/&gt", tag_path(tag) %>
  <% end %>
</span>
